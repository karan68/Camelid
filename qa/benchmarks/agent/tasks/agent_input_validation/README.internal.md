# Input Validation

Project-authored synthetic JavaScript fixture. Visible tests establish valid and range behavior. The controller-owned scorer adds malformed lexical forms that JavaScript's generic number coercion would otherwise accept.