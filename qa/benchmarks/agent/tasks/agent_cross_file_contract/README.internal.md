# Cross-File Contract

Project-authored synthetic JavaScript fixture. The visible outputs stay stable before and after the migration, while the controller-owned scorer verifies the new record shape, every in-fixture caller, and the absence of the stale symbol.