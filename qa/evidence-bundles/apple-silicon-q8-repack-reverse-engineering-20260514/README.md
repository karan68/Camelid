- CAMELID TPM LLAMACPP RE MAC Q8: host/build proof captured for Apple Silicon native arm64: Camelid `6a12d6d6863f389b8b2a4ce897cea3c1bab372c2`, llama.cpp `834a243664114487f99520370a7a7b00fc7a486f`, macOS 26.5/Darwin 25.5.0/Apple M4, `FEAT_DotProd=1`, `FEAT_I8MM=1`; evidence: `logs/re-run-env-camelid-20260514T200654Z.txt`, `logs/re-run-llamacpp-build-runtime-proof-20260514T201648Z.txt`.
- CAMELID TPM LLAMACPP RE MAC Q8: llama.cpp Mac path is CPU-only in this build (`GGML_METAL=OFF`, `-ngl 0`, no Metal dylib; runtime warns no usable GPU), with CPU `NEON=1 | MATMUL_INT8=1 | DOTPROD=1 | SME=1 | ACCELERATE=1 | REPACK=1`; Accelerate/BLAS is linked, but Q8_0 dense fast path is GGML CPU_REPACK + arm kernels, not Metal.
- CAMELID TPM LLAMACPP RE MAC Q8: exact Q8_0 dispatch proven from source: `GGML_TYPE_Q8_0` selects `q8_0_4x8_q8_0` when NEON+MATMUL_INT8 and rows are `%4==0`, otherwise `q8_0_4x4_q8_0` when NEON+DOTPROD and rows are `%4==0`; non-Mac RISC-V 16x1 is not relevant here. Repack is backend-owned `CPU_REPACK`: `set_tensor` repacks directly into tensor data using `block_q8_0x4` storage with 4-row groups and block interleave 4 or 8; evidence: `logs/re-run-llamacpp-q8-path-source-excerpts-20260514T201648Z.txt`.
- CAMELID TPM LLAMACPP RE MAC Q8: Camelid first landed port slice is default-off `CAMELID_MAC_Q8_REPACK=on`. It initially covered Llama 3.2 3B `blk.*.attn_q.weight`; the follow-up technical slice now also covers `blk.*.ffn_gate.weight` and `blk.*.ffn_up.weight`. The loader reads GGUF Q8_0 bytes into backend-owned `Q8_0RuntimeStorage::PackedRows4` 4x8 storage for the gated families; Q8 linear ops consume that packed runtime storage directly. For rectangular FFN gate/up tensors, runtime storage is packed in the transposed output-row order consumed by Camelid's hot Q8 matmul path. The runtime storage path does not retain duplicate row-major Q8 blocks, file backing, or f32 data for the performance copy.
- CAMELID TPM LLAMACPP RE MAC Q8: fallback/default behavior remains unchanged: `CAMELID_MAC_Q8_REPACK` defaults off; legacy `CAMELID_Q8_0_PACKED_4X4_DOT` / `CAMELID_Q8_0_PACKED_4X8_DOT` sidecars remain explicit parity/debug opt-ins only, and `CAMELID_MAC_Q8_REPACK=on` no longer enables those sidecars from `from_f32_with_q8_0_blocks`.
- CAMELID TPM LLAMACPP RE MAC Q8: parity/guard gates pass against current Camelid Q8 retained-block truth and stable release build: `cargo fmt --check`, `cargo test --release -q q8_0 --lib`, `cargo test --release -q mac_q8_repack_loads_attn_q_as_packed_runtime_without_row_major_duplicate`, and `cargo build --release -q`; evidence: `logs/runtime-packed-storage-tests-attn-q-20260514T203935Z.txt`. Earlier sidecar/family-narrowing runs were used during development; final committed evidence is the runtime-owned attn_q slice above.
- CAMELID TPM LLAMACPP RE MAC Q8: same-host Llama 3.2 3B Q8_0 1-token benchmark was rerun after backend-owned runtime storage. Evidence: `logs/same-host-3b-runtime_owned_attn_q_only-1tok-port19231-20260514T204038Z.log.txt`, JSON `logs/same-host-3b-runtime_owned_attn_q_only-1tok-port19231-20260514T204038Z.json`, and final state `logs/final-runtime-owned-attn-q-state-20260514T204141Z.txt`. Trace shows 28 `blk.*.attn_q.weight` tensors packed with `owner=runtime`, total `297,271,296` packed bytes and `133,812 µs` pack time. Benchmark outputs matched the bounded guard text (`Exact`/`Exact`); Camelid TTFT remained slower/noisy (`4555.23 ms` vs llama.cpp `979.23 ms`, `+365.18%`). This is correctness/fallback/architecture evidence, not a speed-success claim.
- CAMELID TPM LLAMACPP RE MAC Q8: current committed head `1dd65f90163123602b97222f92c402320914f2f5` was rechecked after compaction. Evidence: `logs/current-head-grep-evidence-20260514T204932Z.txt` captures fresh focused `git grep` references for llama.cpp Q8_0 repack/MUL_MAT/threading and Camelid runtime-owned packed storage; `logs/current-head-runtime-packed-tests-20260514T204940Z.txt` records `cargo fmt --check`, `cargo test --release -q mac_q8_repack_loads_attn_q_as_packed_runtime_without_row_major_duplicate`, and `cargo test --release -q q8_0_file_backed_packed_rows4_dot_matches_retained_without_q8_blocks --lib` passing.
- CAMELID TPM LLAMACPP RE MAC Q8: follow-up FFN gate/up runtime-owned slice passed focused validation with default behavior still off: `cargo fmt --check`, `cargo test --release -q q8_0 --lib` (43 tests), `cargo test --release -q mac_q8_repack_loads --test tensor_store` (2 tests), and `cargo build --release -q`; evidence: `logs/runtime-packed-storage-tests-attnq-gateup-20260514T205551Z.txt`.
- CAMELID TPM LLAMACPP RE MAC Q8: same-host 3B Q8_0 raw HTML 512-token benchmark with `CAMELID_MAC_Q8_REPACK=on` for attn_q+ffn_gate+ffn_up packed 84 tensors total (28 each family) and preserved the baseline token hash `674f404ffc27265139853544848e9bd44d9631cc583614a45e77e2a134017e52`. Result: wall `49.455s`, generate `49.421s`, `10.360 tok/s`, layers `47.186s`, logits `2.134s`, attention projection `7.335s`, FFN total `26.640s`; evidence: `logs/same-host-3b-runtime_owned_attnq_gateup-512-20260514T205551Z.json`, server trace `logs/same-host-3b-runtime_owned_attnq_gateup-512-20260514T205551Z.server.log.txt`, summary `logs/final-runtime-owned-attnq-gateup-state-20260514T205551Z.json`. Compared with the pre-port Camelid baseline (`54.070s`, `9.469 tok/s`), this is `4.649s` faster / `8.60%` lower generate time and narrows the remaining CPU-only llama.cpp gap (`48.711s`, `10.511 tok/s`) to about `0.71s`.
